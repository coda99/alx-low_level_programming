#include <stdio.h>

/**
 * main -mother of all functions
 *
 * printf: displays message to std out
 *
 * Return: 0 if successful else error message
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
