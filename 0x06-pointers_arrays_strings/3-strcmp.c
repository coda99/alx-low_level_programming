#include "main.h"
#include <string.h>

/**
 * _strcmp - concatinates two strings with n bytes
 *
 * @s1: first string
 * @s2: second string
 *
 * Return: s2 as final result
 */

int _strcmp(char *s1, char *s2)
{
	int result;

	result = strcmp(s1, s2);

	return (result);
}
